t = int(input())

for _ in range(t):
    n, k = map(int, input().split())
    arr = list(map(int, input().split()))

    ans = ""

    for money in arr:
        if k >= money:
            ans += "1"
            k -= money
        else:
            ans += "0"

    print(ans)
