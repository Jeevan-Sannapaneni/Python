t = int(input())

for _ in range(t):
    s = input().strip()

    freq = {}

    for ch in s:
        freq[ch] = freq.get(ch, 0) + 1

    cost = 0

    for x in freq.values():
        cost += (x + 1) // 2

    print(cost)
