
t = int(input())

for _ in range(t):
    a, b = map(int, input().split())

    need = 1

    while True:
        if a < need:
            print("Bob")
            break
        a -= need
        need += 1

        if b < need:
            print("Limak")
            break
        b -= need
        need += 1
