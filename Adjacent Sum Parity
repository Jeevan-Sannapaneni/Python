t = int(input())

for _ in range(t):
    n = int(input())
    arr = list(map(int, input().split()))

    ok = True

    for i in range(n - 1):
        if (arr[i] + arr[i + 1]) % 2 != 0:
            ok = False
            break

    print("YES" if ok else "NO")
