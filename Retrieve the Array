

t = int(input())

for _ in range(t):
    n = int(input())
    b = list(map(int, input().split()))

    s = sum(b) // (n + 1)

    ans = []

    for x in b:
        ans.append(str(x - s))

    print(" ".join(ans))
