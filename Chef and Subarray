# Chef and Subarray

n = int(input())
arr = list(map(int, input().split()))

max_len = 0
count = 0

for x in arr:
    if x != 0:
        count += 1
        max_len = max(max_len, count)
    else:
        count = 0

print(max_len)
